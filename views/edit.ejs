<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Profile</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 min-h-screen flex items-center justify-center p-6">
  <div class="bg-zinc-800 p-8 rounded-xl w-full max-w-md text-white">
    <h1 class="text-2xl font-semibold mb-6 text-center">Edit User Profile</h1>

    <form action="/profile/edit/<%= users._id %>" method="post" class="space-y-5">
      <div>
        <label class="block mb-1 text-sm">Name</label>
        <input type="text" name= "UserName" value="<%= users.UserName %>" required
          class="w-full p-2 rounded bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500" />
      </div>

      <div>
        <label class="block mb-1 text-sm">Email</label>
        <input type="email" name="email" value="<%= users.email %>" required
          class="w-full p-2 rounded bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-500" />
      </div>

      <button type="submit"
        class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 rounded transition">
        Save Changes
      </button>
    </form>
  </div>
</body>
</html>
