<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Vaccination Tracker Form</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-indigo-500 to-purple-600 min-h-screen flex items-center justify-center text-white">

  <div class="bg-zinc-900 p-8 rounded-lg shadow-lg w-full max-w-lg">
    <h2 class="text-3xl font-bold text-center mb-6">Vaccination Tracker</h2>
    
    <form action="/submit-vaccine-data" method="POST" class="space-y-4">
      
      <input
        type="text"
        name="name"
        placeholder="Child's Full Name"
        required
        class="w-full px-4 py-2 rounded bg-zinc-800 border border-purple-500 focus:outline-none focus:ring-2 focus:ring-purple-400"
      />
      
      <input
        type="number"
        name="age"
        placeholder="Child's Age (0 - 16)"
        min="0"
        max="16"
        required
        class="w-full px-4 py-2 rounded bg-zinc-800 border border-purple-500 focus:outline-none focus:ring-2 focus:ring-purple-400"
      />

      <input
        type="date"
        name="dob"
        placeholder="Date of Birth"
        required
        class="w-full px-4 py-2 rounded bg-zinc-800 border border-purple-500 focus:outline-none focus:ring-2 focus:ring-purple-400"
      />

      <input
        type="text"
        name="guardianName"
        placeholder="Guardian's Name"
        required
        class="w-full px-4 py-2 rounded bg-zinc-800 border border-purple-500 focus:outline-none focus:ring-2 focus:ring-purple-400"
      />

      <input
        type="email"
        name="email"
        placeholder="Guardian's Email"
        required
        class="w-full px-4 py-2 rounded bg-zinc-800 border border-purple-500 focus:outline-none focus:ring-2 focus:ring-purple-400"
      />

      <input
        type="tel"
        name="phone"
        placeholder="Phone Number"
        required
        class="w-full px-4 py-2 rounded bg-zinc-800 border border-purple-500 focus:outline-none focus:ring-2 focus:ring-purple-400"
      />

      <textarea
        name="address"
        rows="3"
        placeholder="Address"
        required
        class="w-full px-4 py-2 rounded bg-zinc-800 border border-purple-500 focus:outline-none focus:ring-2 focus:ring-purple-400"
      ></textarea>

      <button
        type="submit"
        class="w-full bg-purple-600 hover:bg-purple-700 text-white font-semibold py-2 rounded transition"
      >
        Submit
      </button>
    </form>
  </div>

</body>
</html>
