<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<style>
    
</style>
<body class="h-screen flex bg-black text-white">

  <!-- Login Form on the Left -->
  <div class="w-full lg:w-1/2 flex items-center justify-center p-8 order-2 lg:order-1">
    <div class="max-w-md w-full bg-zinc-900 p-8 rounded-lg shadow-lg">
      <h2 class="text-2xl font-bold text-center mb-6">Sign In</h2>
      <form action="/signin" method="post" class="space-y-4">
        <input type="email" name="email" placeholder="Email" required
          class="w-full px-4 py-2 bg-zinc-800 text-white border border-zinc-700 rounded focus:outline-none focus:ring-2 focus:ring-purple-500" />
        <input type="password" name="password" placeholder="Password" required
          class="w-full px-4 py-2 bg-zinc-800 text-white border border-zinc-700 rounded focus:outline-none focus:ring-2 focus:ring-purple-500" />
        <button type="submit"
          class="w-full bg-purple-600 text-white py-2 rounded hover:bg-purple-700 transition">
          GET OTP
        </button>
      </form>
      <p class="mt-4 text-center text-sm text-gray-400">
        Don’t have an account?
        <a href="/signup" class="text-purple-400 hover:underline">Sign up</a>
      </p>
      <% if (locals.error) { %>
        <div class="mt-5 w-full p-4 rounded-lg bg-red-400 bg-opacity-10 text-white text-sm font-medium shadow border border-purple-400">
            <%= locals.error %>
          </div>
      <% } %>
    </div>
  </div>

  <!-- Welcome Message on the Right -->
  <div class="w-1/2 hidden lg:flex items-center justify-center bg-purple-800 text-white p-10 order-1 lg:order-2">
    <div class="text-center">
      <h1 class="text-4xl font-bold mb-4">Welcome Back</h1>
      <p class="text-lg text-gray-200">Enter your credentials to log in securely.</p>
    </div>
  </div>

</body>
</html>
